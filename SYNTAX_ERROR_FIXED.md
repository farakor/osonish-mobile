# ✅ Синтаксическая ошибка исправлена!

## 🚀 Что делать сейчас:

### Самое быстрое решение (30 секунд):
1. [supabase.com](https://supabase.com) → ваш проект → **SQL Editor**
2. **New query** → скопируйте и выполните:
```sql
CREATE POLICY "Allow everyone to see buckets" ON storage.buckets FOR SELECT USING (true);
```
3. Нажмите **Run**
4. Перезапустите мобильное приложение

### Если нужна полная настройка:
Используйте исправленный файл `STORAGE_POLICIES_FIX.sql` или `STORAGE_POLICIES_SIMPLE.sql`

## 🎯 Результат:
После выполнения команды в логах должно появиться:
```
[MediaService] 📋 Найдено buckets: 1
[MediaService] ✅ Bucket 'order-media' найден!
```

## 🆘 Если все еще не работает:
Временно отключите Storage в `src/services/mediaService.ts`:
```typescript
private readonly ENABLE_STORAGE = false;
``` 