# 🔧 Исправление загрузки медиа файлов

## 🚨 Проблема

Логи показывают следующие ошибки при загрузке медиа файлов:

```
❌ Ошибка аутентификации: [AuthSessionMissingError: Auth session missing!]
❌ Ошибка загрузки файла: [StorageApiError: new row violates row-level security policy]
```

## 🔍 Причина

В приложении используются **две разные системы аутентификации**:

1. **Собственная система** (AuthService + AsyncStorage) - для основной логики приложения
2. **Supabase Auth** - требуется для Supabase Storage

**Supabase Storage** ожидает валидную Supabase Auth сессию, но пользователи аутентифицированы только в собственной системе.

## ✅ Решения

### Вариант 1: Быстрое исправление (Рекомендуется)

**Настроить анонимную загрузку файлов в Storage:**

1. Откройте Supabase Dashboard → SQL Editor
2. Выполните SQL из файла `STORAGE_POLICIES_ANONYMOUS.sql`:

```sql
-- Разрешить анонимную загрузку в order-media bucket
CREATE POLICY "Allow anonymous upload to order-media" 
ON storage.objects 
FOR INSERT 
WITH CHECK (bucket_id = 'order-media');
```

3. Убедитесь что bucket `order-media` существует и настроен как публичный

### Вариант 2: Долгосрочное решение

**Интегрировать Supabase Auth в AuthService:**

1. Модифицировать AuthService для использования Supabase Auth
2. Синхронизировать пользовательские данные между системами
3. Обновить компоненты для работы с новой системой

## 🧪 Проверка исправления

После применения SQL политик:

1. Создайте новый заказ с медиа файлами
2. Проверьте логи - должны исчезнуть ошибки RLS
3. Файлы должны загружаться в Storage с публичными URL

## 📁 Структура медиа файлов

```
order-media/
└── orders/
    ├── 1735624273001_0.jpg
    ├── 1735624273001_1.mp4
    └── ...
```

## 🔧 Fallback механизм

Если Storage недоступен, приложение автоматически:
- Использует локальные URI файлов
- Показывает предупреждение пользователю
- Продолжает работу без прерывания

## 💡 Дополнительные улучшения

1. **Мониторинг**: Добавить телеметрию для отслеживания успешности загрузок
2. **Оптимизация**: Сжатие изображений перед загрузкой
3. **UX**: Прогресс-бар для загрузки больших файлов
4. **Безопасность**: Валидация типов и размеров файлов на сервере

## 🔗 Связанные файлы

- `src/services/mediaService.ts` - основная логика загрузки
- `src/screens/customer/CreateOrderScreen.tsx` - использование mediaService
- `STORAGE_POLICIES_ANONYMOUS.sql` - SQL для исправления политик
- `STORAGE_POLICIES_FIX.sql` - альтернативные политики (с аутентификацией) 