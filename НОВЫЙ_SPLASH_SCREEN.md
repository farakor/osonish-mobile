# Новый анимированный Splash Screen ✨

## Что изменилось

Заменили старый Lottie-анимированный splash screen на новый кастомный splash с анимацией логотипа Osonish.

## Анимация

### Этапы

1. **Буква "O"** с желтой черточкой появляется с эффектом zoom out (увеличение) по центру экрана
2. Поочередно появляются буквы **"S", "O", "N", "I", "S", "H"**
3. Каждая новая буква сдвигает предыдущие влево
4. В конце появляется **желтая черточка** под первой буквой "O"
5. Получается полноценный логотип **OSONISH**

### Технические детали

- **Длительность**: ~3.7 секунды
- **Библиотека**: react-native-reanimated v4.1.1
- **Графика**: react-native-svg (векторная графика)
- **Эффекты**: Spring-анимация для плавности движений

## Файлы

### Новые файлы
- `src/components/AnimatedLogo.tsx` - компонент с анимацией логотипа

### Изменённые файлы  
- `src/screens/shared/SplashScreen.tsx` - обновлен для использования нового компонента

## Запуск и тестирование

```bash
# Запустить в режиме разработки
npm run start:dev

# Для Android
npm run android:dev

# Для iOS
npm run ios:dev
```

## Настройка

Если нужно изменить скорость или тайминг анимации, отредактируйте таймауты в файле `src/components/AnimatedLogo.tsx`.

### Пример: ускорить анимацию на 50%

```typescript
// Было
setTimeout(() => {
  // код анимации
}, 1000);

// Стало (в 2 раза быстрее)
setTimeout(() => {
  // код анимации  
}, 500);
```

## Цвета логотипа

- 🟢 Зеленый (текст): `#679b00`
- 🟡 Желтый (черточка): `#ffaf00`
- ⚪ Фон: `#F4F5FC`

---

**Автор**: Создано по запросу на основе логотипа `logo-osonish.svg`



