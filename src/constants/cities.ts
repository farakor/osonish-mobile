// Re-export cities from cityUtils for convenience
export { UZBEKISTAN_CITIES, getAllCities, getCityName, isValidCityId } from '../utils/cityUtils';
export type { CityId } from '../utils/cityUtils';

