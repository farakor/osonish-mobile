# 🔔 Push Уведомления - Osonish Mobile

## Быстрый старт

Система push уведомлений полностью реализована и готова к использованию.

### ✅ Что работает сейчас

- **Регистрация push токенов** при входе в приложение
- **Уведомления о новых заказах** → всем исполнителям
- **Уведомления о новых откликах** → заказчику
- **Уведомления о выборе исполнителя** → выбранному исполнителю  
- **Уведомления о завершении заказа** → всем участникам
- **Настройки уведомлений** для пользователей

### 📱 Как тестировать

1. **В симуляторе/эмуляторе**: Логи без фактических уведомлений
2. **На реальном устройстве**: Полная функциональность с push уведомлениями

### 🗂️ Структура файлов

```
src/
├── services/
│   ├── notificationService.ts    # Основной сервис push уведомлений
│   └── orderService.ts          # Интеграция с бизнес-логикой
├── screens/
│   ├── customer/NotificationsScreen.tsx  # Настройки для заказчиков
│   └── worker/NotificationsScreen.tsx    # Настройки для исполнителей
└── types/
    └── index.ts                 # Типы для уведомлений

База данных:
├── push_tokens              # Токены пользователей
├── notification_settings    # Настройки пользователей  
└── notification_logs       # Логи отправленных уведомлений
```

### 🚀 Для продакшена

См. подробную документацию: **[PUSH_NOTIFICATIONS_PRODUCTION_SETUP.md](./PUSH_NOTIFICATIONS_PRODUCTION_SETUP.md)**

**Краткий план:**
1. Получить Apple Developer Account + Google Play Console
2. Настроить APNs и FCM сертификаты
3. Обновить Bundle/Package identifiers
4. Собрать через EAS Build
5. Протестировать на реальных устройствах
6. Опубликовать в магазины

### ⚡ Быстрые команды

```bash
# Разработка
npm start

# Создание preview билда
eas build --platform all --profile preview

# Создание продакшен билда (когда готово)
eas build --platform all --profile production
```

### 🐛 Troubleshooting

**Ошибка "No projectId found"**
- ✅ Исправлено: projectId настроен в app.json

**Уведомления не приходят**
- Проверьте настройки пользователя в экране "Уведомления"
- Убедитесь что тестируете на реальном устройстве
- Проверьте логи в консоли

**Проблемы с базой данных**
- Выполните миграцию: `push_notifications_migration.sql`
- Проверьте подключение к Supabase

### 📊 Мониторинг

Логи уведомлений сохраняются в таблице `notification_logs`:
```sql
SELECT * FROM notification_logs 
WHERE created_at > NOW() - INTERVAL '1 day'
ORDER BY created_at DESC;
```

---

💡 **Подсказка**: Для полного тестирования используйте реальное устройство с настроенными push сертификатами.